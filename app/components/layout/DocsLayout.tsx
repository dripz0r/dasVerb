"use client";
import { ReactNode } from "react";
// import { GrammarContextProvider } from "@/context/grammar-context";

export function DocsLayout({ children }: { children: ReactNode }) {
  // return <GrammarContextProvider>{children}</GrammarContextProvider>;
  return <>{children}</>;
} 